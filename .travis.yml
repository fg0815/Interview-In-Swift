language: objective-c

script:
  - |
    xcodebuild build \
    -project swift-lintcode.xcodeproj \
    -scheme swift-lintcode \
    -sdk iphonesimulator \
    -destination 'platform=iOS Simulator,name=iPhone 6,OS=latest'